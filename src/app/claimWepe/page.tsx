"use client";

import Layout from "@/components/custom/server/Layout";
import ClaimWepe from "@/components/custom/client/ClaimWepe";

const ClaimWepePage = () => {
  return (
    <Layout>
      <ClaimWepe/>
    </Layout>
  );
};

export default ClaimWepePage;
