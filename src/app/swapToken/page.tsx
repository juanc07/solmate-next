import React from 'react'
import Layout from "@/components/custom/server/Layout"
import About from "@/components/custom/client/About"
import SwapToken from '@/components/custom/client/SwapToken'

const SwapTokenPage = () => {
  return (
    <Layout>
    <SwapToken/>    
    </Layout>
  )
}

export default SwapTokenPage